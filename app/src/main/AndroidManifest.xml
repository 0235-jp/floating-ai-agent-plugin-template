<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <permission android:name="jp.co.u0235.floating_ai_agent.permission.TOOLS_READ" />
    <uses-permission android:name="jp.co.u0235.floating_ai_agent.permission.TOOLS_READ" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication"
        tools:targetApi="31">
        <provider
            android:name=".TemplateContentProvider"
            android:authorities="jp.co.u0235.floating-ai-agent-plugin.template.provider"
            android:enabled="true"
            android:exported="true"
            android:readPermission="jp.co.u0235.floating_ai_agent.permission.TOOLS_READ">
            <intent-filter>
                <action android:name="jp.co.u0235.floating_ai_agent.ACTION_TOOLS" />
            </intent-filter>
        </provider>
    </application>

</manifest>